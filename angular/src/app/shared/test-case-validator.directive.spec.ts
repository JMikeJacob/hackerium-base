import { TestCaseValidatorDirective } from './test-case-validator.directive';

describe('TestCaseValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TestCaseValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
