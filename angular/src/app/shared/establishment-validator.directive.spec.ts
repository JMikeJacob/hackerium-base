import { EstablishmentValidatorDirective } from './establishment-validator.directive';

describe('EstablishmentValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EstablishmentValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
