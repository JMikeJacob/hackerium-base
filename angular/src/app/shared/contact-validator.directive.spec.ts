import { ContactValidatorDirective } from './contact-validator.directive';

describe('ContactValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
